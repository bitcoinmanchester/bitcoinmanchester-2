
##############
# Jekyll tasks
##############

# Usage: rake serve
desc "Serve Jekyll site locally"
task :serve do
  # Changed this to use --watch, no idea why the original didn't
  # system "bundle exec jekyll serve --no-watch --config _config.yml"
  system "bundle exec jekyll serve --watch --config _config.yml"
end # task :serve
